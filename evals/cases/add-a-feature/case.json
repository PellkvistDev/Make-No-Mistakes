{
  "task": "Add a `slugify(text)` function to slug.py. It must lowercase the text, replace any run of non-alphanumeric characters with a single hyphen, and strip leading and trailing hyphens. The tests in test_slug.py describe it exactly.",
  "check": "python -m pytest -q",
  "protect": ["test_slug.py"],
  "timeout": 180
}
